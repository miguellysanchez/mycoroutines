<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sample.mycoroutines">

    <application
        android:name=".MyCoroutinesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".p1_intro.ThreadVsCoroutineActivity"
            android:label="Threads vs. Coroutines" />
        <activity
            android:name=".p2_suspend.SuspendFunctionsActivity"
            android:label="Suspend Functions" />
        <activity
            android:name=".p3_builders.CoroutineBuildersActivity"
            android:label="Suspend Functions" />
        <activity
            android:name=".p4_jobs_and_cancellation.CancelCoroutineActivity"
            android:label="Cancelling Coroutines" />

        <activity
            android:name=".p5_context_dispatchers.DispatcherDemoActivity"
            android:label="Contexts and Dispatchers" />
        <activity
            android:name=".SandboxActivity"
            android:label="Sandbox Test" />
    </application>

</manifest>